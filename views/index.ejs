<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CAROMAR - Copy A Repository Or Merge All Repositories</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <div class="container">
            <h1><i class="fab fa-github"></i> CAROMAR</h1>
            <p>Copy A Repository Or Merge All Repositories</p>
        </div>
    </header>

    <main class="container">
        <!-- Authentication Section -->
        <section id="auth-section" class="card">
            <h2><i class="fas fa-key"></i> GitHub Authentication</h2>
            <div class="auth-info">
                <p>To use CAROMAR, you need to provide a GitHub Personal Access Token.</p>
                <a href="https://github.com/settings/tokens" target="_blank" class="link">
                    <i class="fas fa-external-link-alt"></i> Create Personal Access Token
                </a>
            </div>
            <div class="input-group">
                <input type="password" id="github-token" placeholder="Enter your GitHub Personal Access Token" />
                <button id="validate-token" class="btn btn-primary">
                    <i class="fas fa-check"></i> Validate Token
                </button>
            </div>
            <div id="user-info" class="user-info" style="display: none;">
                <img id="user-avatar" src="" alt="User Avatar">
                <div>
                    <strong id="user-name"></strong>
                    <p id="user-username"></p>
                </div>
            </div>
        </section>

        <!-- Repository Search Section -->
        <section id="repo-section" class="card" style="display: none;">
            <h2><i class="fas fa-search"></i> Find Repositories</h2>
            <div class="input-group">
                <input type="text" id="username-input" placeholder="Enter GitHub username" />
                <button id="search-repos" class="btn btn-secondary">
                    <i class="fas fa-search"></i> Search Repositories
                </button>
            </div>
        </section>

        <!-- Repository List Section -->
        <section id="repos-list-section" class="card" style="display: none;">
            <div class="section-header">
                <h2><i class="fas fa-list"></i> Select Repositories</h2>
                <div class="action-buttons">
                    <button id="select-all" class="btn btn-small">
                        <i class="fas fa-check-double"></i> Select All
                    </button>
                    <button id="deselect-all" class="btn btn-small">
                        <i class="fas fa-times"></i> Deselect All
                    </button>
                </div>
            </div>
            
            <div class="operation-modes">
                <h3>Choose Operation:</h3>
                <div class="radio-group">
                    <label>
                        <input type="radio" name="operation" value="fork" checked>
                        <i class="fas fa-code-branch"></i> Fork Individual Repositories
                        <span class="description">Fork each selected repository individually to your account</span>
                    </label>
                    <label>
                        <input type="radio" name="operation" value="merge">
                        <i class="fas fa-layer-group"></i> Merge into Single Repository
                        <span class="description">Create a single repository with all selected repos as folders</span>
                    </label>
                </div>
            </div>

            <div id="merge-options" style="display: none;">
                <div class="input-group">
                    <input type="text" id="merged-repo-name" placeholder="Enter name for merged repository" />
                </div>
            </div>

            <div id="repos-list" class="repos-grid">
                <!-- Repositories will be loaded here -->
            </div>

            <div class="action-section">
                <button id="execute-action" class="btn btn-success" disabled>
                    <i class="fas fa-play"></i> Execute Selected Action
                </button>
                <div id="progress-section" style="display: none;">
                    <div class="progress-bar">
                        <div id="progress-fill"></div>
                    </div>
                    <div id="progress-text"></div>
                </div>
            </div>
        </section>

        <!-- Results Section -->
        <section id="results-section" class="card" style="display: none;">
            <h2><i class="fas fa-check-circle"></i> Results</h2>
            <div id="results-content">
                <!-- Results will be displayed here -->
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2024 CAROMAR. A tool for GitHub repository management.</p>
        </div>
    </footer>

    <script src="/js/app.js"></script>
</body>
</html>